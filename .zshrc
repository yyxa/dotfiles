alias code="code --ozone-platform=wayland"

unsetopt BEEP

# alias nekoshow='hyprctl dispatch focuswindow class:nekoray; hyprctl dispatch opacity 1.0 override 1.0, class:nekoray'
# alias nekohide='hyprctl dispatch opacity 0.0 override 0.0, class:Nenoray'

eval "$(starship init zsh)"

